//Name: Aiden Krasnopolskiy
//Language: Java
//IDE: VS Code

import java.util.Arrays;
public class HW2 {
    public static void main (String [] args) {
        // Original array and copies for sorting and searching
        int [] arr1 = {56, 79, 60, 32, 64, 12, 10, 91, 41, 80, 72, 39};
        int [] arr2 = Arrays.copyOf(arr1, arr1.length);
        int [] arr3 = Arrays.copyOf(arr1, arr1.length);
        System.out.println("Original array:");
        for (int i = 0; i < arr1.length; i++) {
            System.out.print(arr1[i] + " ");
        }
        System.out.println("\n");
        //Bubble Sort output
        bubbleSort(arr1);
        System.out.println("Bubble Sort Result:");
        for (int i = 0; i < arr1.length; i++) {
            System.out.print(arr1[i] + " ");
        }
        System.out.println("\n");
        //Merge Sort output
        mergeSort(arr2, 0, arr2.length - 1);
        System.out.println("Merge Sort Result:");
        for (int i = 0; i < arr2.length; i++) {
            System.out.print(arr2[i] + " ");
        }
        System.out.println("\n");
        //Linear Search output
        System.out.println("Linear Search Result:" );
        System.out.println("Target: " + 60);
        int result = linearSearch(arr3, 60);
        if (result == -1) {
            System.out.println("Target not found.");
        } 
        else {
            System.out.println("Target found at index: " + result);
        }
        System.out.println("\nLinear Search Result:" );
        System.out.println("Target: " + 39);
        int result2 = linearSearch(arr3, 39);
        if (result2 == -1) {
            System.out.println("Target not found.");
        } 
        else {
            System.out.println("Target found at index: " + result2);
        }
        System.out.println("\nLinear Search Result:" );
        System.out.println("Target: " + 100);
        int result3 = linearSearch(arr3, 100);
        if (result3 == -1) {
            System.out.println("Target not found.");
        } 
        else {
            System.out.println("Target found at index: " + result3);
        }
        //Binary Search output
        System.out.println("\nBinary Search Result:" );
        System.out.println("Target: " + 91);
        int result4 = binarySearch(arr1, 91);
        if (result4 == -1) {
            System.out.println("Target not found.");
        } 
        else {
            System.out.println("Target found at index: " + result4);
        }
        System.out.println("\nBinary Search Result:" );
        System.out.println("Target: " + 12);
        int result5 = binarySearch(arr1, 12);
        if (result5 == -1) {
            System.out.println("Target not found.");
        } 
        else {
            System.out.println("Target found at index: " + result5);
        }
        System.out.println("\nBinary Search Result:" );
        System.out.println("Target: " + 100);
        int result6 = binarySearch(arr1, 100);
        if (result6 == -1) {
            System.out.println("Target not found.");
        } 
        else {
            System.out.println("Target found at index: " + result6);
        }
    }
    // Bubble Sort
    public static void bubbleSort(int [] arr) {
        for (int i = 0; i < arr.length - 1; i++) {
            for (int j = 0; j < arr.length - i - 1; j++) {
                if (arr[j] > arr[j + 1]) {
                    int temp = arr[j];
                    arr[j] = arr[j + 1];
                    arr[j + 1] = temp;
                }
            }
        }
    }
    //Merge Sort
    public static void mergeSort(int [] arr, int left, int right) {
        if (left < right) {
            int mid = (left + right) / 2;
            mergeSort(arr, left, mid);
            mergeSort(arr, mid + 1, right);
            merge(arr, left, mid, right);
        }
    }
    // Merge function for Merge Sort
    public static void merge(int [] arr, int left, int mid, int right) {
        int n1 = mid - left + 1;
        int n2 = right - mid;
        int [] L = new int[n1];
        int [] R = new int[n2];
        for (int i = 0; i < n1; i++) {
            L[i] = arr[left + i];
        }
        for (int j = 0; j < n2; j++) {
            R[j] = arr[mid + 1 + j];
        }
        int i = 0, j = 0;
        int k = left;
        while (i < n1 && j < n2) {
            if (L[i] <= R[j]) {
                arr[k] = L[i];
                i++;
            } else {
                arr[k] = R[j];
                j++;
            }
            k++;
        }
        while (i < n1) {
            arr[k] = L[i];
            i++;
            k++;
        }
        while (j < n2) {
            arr[k] = R[j];
            j++;
            k++;
        }
    }
    //Linear Search
    public static int linearSearch(int [] arr, int num) {
        for (int i = 0; i < arr.length; i++) {
            if (arr[i] == num) {
                return i;
            }
        }
        return -1;
    }
    //Binary Search
    public static int binarySearch(int [] arr, int num) {
        int left = 0;
        int right = arr.length - 1;
        while (left <= right) {
            int mid = left + (right - left) / 2;
            if (arr[mid] == num) {
                return mid;
            }
            if (arr[mid] < num) {
                left = mid + 1;
            } else {
                right = mid - 1;
            }
        }
        return -1;
    }
}
